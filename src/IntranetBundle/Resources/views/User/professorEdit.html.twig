{% extends "::base.html.twig" %}

{% block title %}{{'Modifier profil - ' ~ user.firstname ~ ' ' ~ user.lastname}}{% endblock %}

{% block body %}

    <h1>{{user.firstname ~ ' ' ~ user.lastname}}</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('student_show') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}